[project]
name = "agentic-development-playbook"
version = "1.2.2"
description = "Spec-driven workflow for AI coding agents with the repo as the source of truth"
readme = "README.md"
requires-python = ">=3.12"
