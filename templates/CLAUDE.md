# CLAUDE.md

This repo uses `AGENTS.md` as the canonical agent instruction file.
Read `AGENTS.md` first and follow it strictly.

Key rules (summary):
- Only execute tasks with a task card in `tasks/`.
- Follow task scope exactly; no drive-by refactors.
- Run Verify commands before the final commit.
- One task = one final commit; commit message must include `Task: T-XXX`.
- Update `TASKS.md` after every task.
